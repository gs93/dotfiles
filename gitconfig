[user]
    name = Giuliano Schneider
    email = gs93@gmx.net

[push]
    default = simple

[color]
    ui = auto

[credential]
    helper = store

[core]
    excludesfile = ~/.gitignore_global

[rebase]
    stat = true

[alias]
    c   = commit -m
    co  = commit
    a   = add
    b   = checkout
    d   = diff
    ds  = diff --stat
    s   = status
    p   = push
    pa  = push --all
    ll  = pull
    ff  = merge --ff-only
    staged  = diff --cached

[branch]
    autosetuprebase = always
